{
  "name": "tienda-mabel-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "prisma migrate deploy && node server.js",
    "postinstall": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "bcryptjs": "^3.0.2",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mercadopago": "^2.8.0",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.6",
    "prisma": "5.22.0"
  },
  "engines": {
    "node": ">=18"
  }
}
